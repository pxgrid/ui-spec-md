module.exports = {
  r: {
    alias: 'rootDir',
    demandOption: false,
    requiresArg: true,
    describe: 'Path of directory to server root. If it is omitted, destDir will be rootDir.',
    type: 'string',
  },
}
